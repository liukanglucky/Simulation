<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.dao.UserDao">

	<insert id="insertUser" parameterType="User" keyProperty="id">
		insert into t_users (id,name,auth,pwd)
		values
		(s_users.nextval,#{name},#{auth},#{pwd})
	</insert>
	
	<update id="updateUser" parameterType="User">
		update t_users set name=#{name},auth=#{auth},pwd=#{pwd} where id=#{id}
	</update>
	
	<select id="findAllUser" resultType="User"> 
        select rownum as rn,t.* from t_users t
    </select> 
    
    <select id="countUser" resultType="count">
    	select count(*) as recordCount from t_users
    </select>
    <select id="findUsersByPage" resultType="User" parameterType="page">
    	select id,name,auth,pwd from 
    	(select rownum as rn,t.* from t_users t where rownum &lt; = #{P1})
    	 where rn  &gt; #{P2}
    </select>
    
    <select id="findUsersByName" parameterType="String" resultType="User">
    	select id,name,auth,pwd from t_users t where 
    	t.name = #{name}
    </select>
    
    <select id="findUsersById" parameterType="int" resultType="User">
    	select * from t_users where id=#{id}
    </select>
    
    <delete id="delete" parameterType="int">
    	delete t_users where id=#{id}
    </delete>
    
    
</mapper>